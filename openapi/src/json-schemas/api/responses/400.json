{
    "type": "object",
    "required": ["error", "errorType"],
    "properties": {
        "error": {
            "type": "string"
        },
        "errorType": {
            "enum": ["invalidRequestError"]
        }
    }
}
