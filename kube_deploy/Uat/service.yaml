apiVersion: v1
kind: Service
metadata:
  name: experian-bankwizard-service-uat
  labels:
    app: experian-bankwizard-service-app-uat
spec:
  ports:
    - port: 80
      name: http
      targetPort: 3800
  selector:
    app: experian-bankwizard-service-app-uat
